(define-fun (gcd a b)
; this is a comment
(if (= a b) a
   (if (> a b) (gcd b (- a b))
    (gcd a (- b a)))))
 (print (gcd (get-int) (get-int)))
(define-fun (gcd a b)
; this is a comment
(if (= a b) a
   (if (> a b) (gcd b (- a b))
    (gcd a (- b a)))))
 (print (gcd (get-int) (get-int)))
 (define-fun (gcd a b)
; this is a comment
(if (= a b) a
   (if (> a b) (gcd b (- a b))
    (gcd a (- b a)))))
 (print (gcd (get-int) (get-int)))
 (define-fun (gcd a b)
; this is a comment
(if (= a b) a
   (if (> a b) (gcd b (- a b))
    (gcd a (- b a)))))
 (print (gcd (get-int) (get-int)))
 
 
 
 
